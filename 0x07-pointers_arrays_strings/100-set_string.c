#include "main.h"

/**
  * set_string - the entry point
  * @s: a pointer to a pointer to a char
  * @to: a pointer to a char
  * Return: 0 (success)
  */

void set_string(char **s, char *to)
{
	*s = to;
}
